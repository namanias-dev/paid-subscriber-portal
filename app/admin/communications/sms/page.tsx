import MissionControl from "@/components/admin/sms/MissionControl";

export const dynamic = "force-dynamic";
export const metadata = { title: "SMS Mission Control — Admin" };

export default function SmsMissionControlPage() {
  return <MissionControl />;
}
