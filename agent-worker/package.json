{
  "name": "naman-ias-agent-worker",
  "version": "0.1.0",
  "private": true,
  "description": "OPTIONAL local OpenClaw/Ollama worker for the Naman IAS AI Counselor Agent. Runs OUTSIDE Vercel — connects to the portal only via HMAC-signed HTTPS. Never affects the live website.",
  "type": "module",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "once": "tsx src/index.ts --once",
    "ping": "tsx src/index.ts --task=ping --once"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "tsx": "^4.22.4",
    "typescript": "^5.6.3"
  }
}
