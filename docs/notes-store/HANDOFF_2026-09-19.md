# Notes Store handoff — 2026-09-19

## Snapshot

| Field | Value |
|-------|-------|
| Branch | `notes-store-release-hardening` |
| Tag | `notes-store-handoff-2026-09-19` |
| Pre-docs HEAD | `f249569ba83b815fd842bc5a5a47a98f9fc351e6` |
| Payment-critical commit | `77230d83456021b4ef20efbe7d80acb6974e453b` |
| Intelligence commit | `9cdba05ed3e032309057838c0e94154328c0f68e` |
| Final handoff commit | `6b668752af8e2cba48f1d9f853a327f493099f8a` |
| Production store | **disabled** |
| Real payment | **`deferred_by_owner`** — not performed |

## Implementation summary

Phase 1 Notes Store: guest checkout, isolated `store_*` ledger, shared Eazypay return URL with `NIASN-N-` dispatch, Verify-as-terminal, hash-only order access tokens, ~90s confirmation polling, 15-minute Verify cron, manual admin fulfilment, preview-only enable. Academy identity/money tables untouched by design + CI guard.

## Built / verified / deferred

See `CURRENT_STATE.md`. Automated: isolation, watermark, access-token, preview-flag, callback-shim, typecheck. **Not** verified: live Eazypay charge, DLT send, Shiprocket, valid Lighthouse on store pages, Playwright.

## Invalid evidence (do not reuse)

Lighthouse scores captured against Vercel SSO login pages are **not** Notes Store scores.

## Test SKU

`TEST-NOTES-POLITY-001` / id `a1dcaa29-bc07-49ae-b810-2854e24d8d59` / ₹1 / on_hand 2 at handoff baseline. Preview-only intent.

## First five next tasks (no real payment required)

1. Authenticated preview browser QA (mobile + desktop) up to payment handoff page.  
2. Upload labelled TEST cover/sample media for the test SKU.  
3. Admin dry-run: queue visibility, advance, ship UI on a safe order.  
4. Local Lighthouse against `next start` with store preview enable.  
5. Submit DLT templates when ops ready (keep SMS flag off until wired).

## External auth for new laptop

GitHub, Vercel, Supabase (see `EXTERNAL_DEPENDENCIES.md`). Pull env via CLI — never commit secrets.

## Continuity rule

Implementation exists and automated isolation tests pass; a real Eazypay transaction remains deliberately deferred and unverified by owner decision.
