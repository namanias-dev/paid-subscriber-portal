# Notes Store — external dependencies

Cannot be completed from code alone:

| Dependency | Status |
|------------|--------|
| Real ICICI Eazypay transaction | **Deferred by owner** |
| DLT template submission/approval (TRAI) | Drafted in `docs/notes-store-dlt-templates.md`; not submitted |
| ICICI SubMerchantId echo confirmation in live store traffic | Unverified by live store payment |
| Shiprocket (or other) credentials | Not Phase 1; flag off |
| GST/HSN final CA position | Schema defaults exempt; confirm with CA |
| Real product photography / sample scans | Content ops |
| Vercel Deployment Protection / SSO for browser QA | Blocks unauthenticated Lighthouse |
| Owner decision to enable production flag | Required before public launch |

Accounts a new laptop may need: GitHub (`namanias-dev/paid-subscriber-portal`), Vercel (`naman-ias`), Supabase project, optional Telegram ops — **authenticate via official CLIs; never paste secrets into docs.**
